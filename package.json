{
  "name": "ltbase-ts",
  "version": "0.1.0",
  "type": "module",
  "description": "TypeScript SDK for LTBase (Bun runtime)",
  "license": "MIT",
  "scripts": {
    "check": "bunx --bun tsc --noEmit",
    "cli": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "typescript": "^5.3.3"
  }
}
